{
  "name": "jarvis",
  "module": "src/server.ts",
  "scripts": {
    "start": "bun src/server.ts",
    "dev": "bash -c '[ -f certs/key.pem ] || bash scripts/generate-cert.sh' && HTTPS=true bun --watch src/server.ts",
    "setup": "bash scripts/generate-cert.sh && echo '\\nStarting server with HTTPS...\\n' && HTTPS=true bun start"
  },
  "dependencies": {
    "headroom-ai": "^0.22.4",
    "puppeteer": "^25.1.0"
  }
}
